---
paths:
  - "**/*.md"
  - "**/*.ts"
  - "**/*.js"
---

# 全局行为规则

> 加载时机：SessionStart（会话开始时）
> 适用范围：所有 AI 操作

## 上下文窗口管理

当上下文窗口接近限制时，系统会自动压缩。不要因为 token 预算问题而提前停止任务。

## 行动优先原则

默认情况下，直接实施更改，而不是仅仅提出建议。

## 并行工具调用

如果多个工具调用之间没有依赖关系，应在同一消息中并行调用所有独立的工具。

## 调查后再回答

永远不要对未打开的代码进行推测。必须先读取相关文件再回答。

## 避免过度工程

避免过度工程化。只做直接请求的或明显必要的更改。

## 临时文件清理

如果创建了任何临时文件或辅助文件，必须在任务结束时清理它们。
